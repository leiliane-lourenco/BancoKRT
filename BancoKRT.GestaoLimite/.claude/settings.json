{
  "permissions": {
    "allow": [
      "Bash(gh auth *)",
      "PowerShell($gh = Get-Command gh -ErrorAction SilentlyContinue; if \\($gh\\) { \"gh: $\\($gh.Source\\)\"; gh auth status } else { \"gh NOT found\" }; \"---git---\"; git config --global user.name; git config --global user.email)"
    ]
  }
}
